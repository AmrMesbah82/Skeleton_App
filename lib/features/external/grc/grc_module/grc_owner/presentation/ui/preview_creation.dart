import 'package:flutter/material.dart';
import 'package:get/get.dart';
class PreviewCreation extends StatelessWidget {
  const PreviewCreation({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Preview'.tr)));
}

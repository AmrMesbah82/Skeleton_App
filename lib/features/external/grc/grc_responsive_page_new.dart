import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: GrcResponsivePageRefactor — GRC module not included in demo_app.
class GrcResponsivePageRefactor extends StatelessWidget {
  const GrcResponsivePageRefactor({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('GRC'.tr)));
}

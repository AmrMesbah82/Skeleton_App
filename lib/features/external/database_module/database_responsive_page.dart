import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: DataBaseIntroPage — database module not included in demo_app.
class DataBaseIntroPage extends StatelessWidget {
  const DataBaseIntroPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Database'.tr)));
}

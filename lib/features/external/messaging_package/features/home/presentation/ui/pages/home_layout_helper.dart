import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: HomePageHelper — module not included in demo_app.
class HomePageHelper extends StatelessWidget {
  const HomePageHelper({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('HomePageHelper'.tr)));
}

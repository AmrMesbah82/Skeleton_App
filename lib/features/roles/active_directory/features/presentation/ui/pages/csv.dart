import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: CsvView — active directory module not included in demo_app.
class CsvView extends StatelessWidget {
  const CsvView({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('CSV Import'.tr)));
}

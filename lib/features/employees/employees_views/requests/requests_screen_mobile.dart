import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: RequestsScreenMobile — full requests screen not included in demo_app.
class RequestsScreenMobile extends StatelessWidget {
  const RequestsScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Requests'.tr)),
    );
  }
}

import 'package:flutter/material.dart';

/// Stub — job posts module not included in demo_app.
class CandidatesDetailsScreenMobile extends StatelessWidget {
  const CandidatesDetailsScreenMobile({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Candidate Details')));
}

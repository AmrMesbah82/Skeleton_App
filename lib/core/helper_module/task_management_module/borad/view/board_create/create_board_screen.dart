import 'package:flutter/material.dart';
import 'package:get/get.dart';
class CreateBoardScreenMobile extends StatelessWidget {
  const CreateBoardScreenMobile({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Create Board'.tr)));
}

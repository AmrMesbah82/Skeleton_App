import 'package:flutter/material.dart';
import 'package:get/get.dart';

// Stub: EmployeeHrAddEmployee — full add-employee screen not included in demo_app.
class EmployeeHrAddEmployee extends StatelessWidget {
  const EmployeeHrAddEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Add Employee'.tr)),
    );
  }
}

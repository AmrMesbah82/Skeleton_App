import 'package:flutter/material.dart';

/// Stub — job posts module not included in demo_app.
class CandidatesApplicationsScreenMobile extends StatelessWidget {
  const CandidatesApplicationsScreenMobile({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Candidates Applications')));
}
